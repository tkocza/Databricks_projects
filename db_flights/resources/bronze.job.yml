resources:
  jobs:
    bronze_ingestion:
      name: bronze_ingestion

      tasks:
        - task_key: ingest

          #spark_python_task:
          #  python_file: ../src/bronze/file.py
          python_wheel_task:
            package_name: db_flights
            entry_point: bronze_ingestion


          environment_key: default

      environments:
        - environment_key: default
          spec:
            environment_version: "2"
            dependencies:
              - ../dist/*.whl

