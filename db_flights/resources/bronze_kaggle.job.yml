resources:
  jobs:
    bronze_kaggle:
      name: Load Kaggle data

      tasks:
        - task_key: kaggle_data

          #spark_python_task:
          #  python_file: ../src/bronze/file.py
          python_wheel_task:
            package_name: db_flights
            entry_point: bronze_kaggle


          environment_key: default

      environments:
        - environment_key: default
          spec:
            environment_version: "2"
            dependencies:
              - ../dist/*.whl

